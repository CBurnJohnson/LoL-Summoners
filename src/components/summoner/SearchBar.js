import React from 'react';

const SearchBar = () => {
    return (
        <div>
            <input type='text' placeholder='Search for Summoner...' />
        </div>
    );
};

export default SearchBar;
